[project]
name = "magyarpolitika-backend"
version = "0.1.0"
description = "Backend application"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

